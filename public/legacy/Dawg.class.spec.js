import Dawg from './Dawg.class';

test('adds 1 + 2 to equal 3', () => {
    console.log(Dawg);
    const bob = new Dawg("bob");
    // expect(sum(1, 2)).toBe(3);
    expect(bob.getName()).toBe(3);
  });